{
  "name": "@cloudflare/agents-mcp-server",
  "description": "zero config stateless MCP Server on Cloudflare",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "bun run types && bun x wrangler deploy",
    "types": "bun x wrangler types env.d.ts --include-runtime false"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.5.0",
    "agents": "*",
    "drizzle-orm": "^0.30.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "drizzle-kit": "^0.21.0",
    "typescript": "^5.4.5",
    "wrangler": "^3.57.1"
  }
}
